services:
  jlm_aws.credentials_provider:
    class: JLM\AwsBundle\Tests\Fixtures\MockService\CredentialsProvider

jlm_aws:
  default_settings:
    credentials:
      key: MY_UNUSED_KEY
      secret: MY_UNUSED_SECRET
      provider_service: "@jlm_aws.credentials_provider"
